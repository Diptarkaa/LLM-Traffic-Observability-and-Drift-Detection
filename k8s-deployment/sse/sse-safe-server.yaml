apiVersion: apps/v1
kind: Deployment
metadata:
  name: sse-safe-app
  namespace: llmg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sse-safe-app
  template:
    metadata:
      labels:
        app: sse-safe-app
    spec:
      containers:
      - name: sse-app
        image: owaap.azurecr.io/pugr/sse-echo-server:latest
        args:
          - "--port=8080"
          - "--interval=1s"
        ports:
        - containerPort: 8080
          name: h2c

---

apiVersion: v1
kind: Service
metadata:
  name: sse-safe-app-svc
  namespace: llmg
spec:
  selector:
    app: sse-safe-app
  ports:
  - name: h2c-port
    port: 8080
    targetPort: 8080
    appProtocol: h2c
