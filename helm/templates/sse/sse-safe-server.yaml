apiVersion: apps/v1
kind: Deployment
metadata:
  name: sse-safe-app
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.sse.safe.replicas }}
  selector:
    matchLabels:
      app: sse-safe-app
  template:
    metadata:
      labels:
        app: sse-safe-app
    spec:
      containers:
      - name: sse-app
        image: {{ .Values.images.ssEchoServer }}
        args:
          - "--port=8080"
          - "--interval={{ .Values.sse.safe.interval }}"
        ports:
        - containerPort: 8080
          name: h2c

---

apiVersion: v1
kind: Service
metadata:
  name: sse-safe-app-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: sse-safe-app
  ports:
  - name: h2c-port
    port: 8080
    targetPort: 8080
    appProtocol: h2c
