apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agentic-protection.fullname" . }}-h2cll-malicious
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.h2cll.malicious.replicas }}
  selector:
    matchLabels:
      app: {{ include "agentic-protection.fullname" . }}-h2cll-malicious
  template:
    metadata:
      labels:
        app: {{ include "agentic-protection.fullname" . }}-h2cll-malicious
    spec:
      containers:
      - name: h2cll-app
        image: {{ .Values.images.h2cll }}
        args:
          - "--port=8080"
          - "--interval={{ .Values.h2cll.malicious.interval }}"
          - "--malicious"
        ports:
        - containerPort: 8080
          name: h2c

---

apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentic-protection.fullname" . }}-h2cll-malicious-svc
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ include "agentic-protection.fullname" . }}-h2cll-malicious
  ports:
  - name: h2c-port
    port: 8080
    targetPort: 8080
    appProtocol: h2c
